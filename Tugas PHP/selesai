<?php
require 'functions.php';

$id = $_GET["id"];

// Ubah status to-do menjadi selesai (status = 1)
if (ubahStatus($id, 1) > 0) {
    header("Location: index.php");
} else {
    echo "<script>
        alert('Gagal mengubah status');
        window.history.back();
    </script>";
}
?>
